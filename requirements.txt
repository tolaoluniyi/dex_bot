requests==2.31.0       # For making HTTP requests to Dexscreener, Rugcheck, and Bitget APIs
pyyaml==6.0.1          # For parsing the YAML configuration file
python-telegram-bot==20.5  # For interacting with the Telegram Bot API
hmac==0.0.0            # For generating HMAC signatures (included in Python standard library)
hashlib==0.0.0         # For generating SHA256 hashes (included in Python standard library)
urllib3==2.0.7         # For URL encoding (included in Python standard library)
logging==0.0.0         # For logging (included in Python standard library)
python-dotenv==1.0.0   # For loading environment variables from a .env file
